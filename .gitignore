# 本地 Token 文件（含敏感信息，不要提交到仓库）
token.txt
GITHUB_PAT.txt

# 船态缓存及禁用标记（GitHub Actions cache 管理）
.last_vessel_state.json
.disabled

# 本地 SMTP 密码
.smtp.txt

# 本地 Webhook 配置
.webhook.txt

# Python 虚拟环境
.venv/
venv/

# Python 缓存
__pycache__/
*.pyc
*.pyo

# Playwright 浏览器缓存
ms-playwright/

# Excel 输出文件（由 Action 生成，自动作为 artifact 保存）
船舶动态跟踪.xlsx

# 系统文件
.DS_Store
Thumbs.db
